import { ArticleFiltrePlatMenuPipe } from './article-filtre-plat-menu.pipe';

describe('ArticleFiltrePlatMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleFiltrePlatMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
