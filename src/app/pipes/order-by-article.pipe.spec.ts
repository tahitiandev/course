import { OrderByArticlePipe } from './order-by-article.pipe';

describe('OrderByArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
