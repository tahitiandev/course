import { ArticleFiltrePipe } from './article-filtre.pipe';

describe('ArticleFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
