import { ArticleFiltrePlatPipe } from './article-filtre-plat.pipe';

describe('ArticleFiltrePlatPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleFiltrePlatPipe();
    expect(pipe).toBeTruthy();
  });
});
