import { ArticleDetailsPipe } from './article-details.pipe';

describe('ArticleDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
